<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlinestorekotlin">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/pp"
        android:label="Umiam Store"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineStoreKotlin"
        android:usesCleartextTraffic="true">
        <activity android:name=".ThankYou"></activity>
        <activity android:name=".Payment_Activity" />
        <activity android:name=".ContactUsActivity" />
        <activity android:name=".PeopleOrdersList" />
        <activity android:name=".PeopleOrders" />
        <activity android:name=".FinalizeShoppingActivity" />
        <activity android:name=".CartProductsActivity" />
        <activity android:name=".FetchProductsActivity" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".SignUpLayout" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>